import { CSSProperties } from "react";

const wrapper: CSSProperties = {
  color: "DarkSlateGray "
};

const header: CSSProperties = {
  color: "black",
  fontSize: "2em"
};

const App = {
  wrapper,
  header
};

export default App;
